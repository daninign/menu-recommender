Concrete issues that have to be solved:

- All drinks are in the Salads category
- 'Kinderijsje' is fish according to the data
- There are no dessert dishes, might be because there aren't any, but something to look into.

Potential improvements:

- Right now it is a price oriented filtering, maybe we could change it/add a scoring mechanism
that takes into account other factors as well?
- Maybe some UI improvements, since right now it just returns a row from the dataset.
